# Overview
This repository is mostly a web API for a train network that runs off of JMRI. JMRI runs the scripts in the scripts folder as Jython, using it's internal fast clock to send signals to the API to change lighting colors in a train layout.

# UI
There is a small UI to configure and debug the lighting system.
